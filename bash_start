# 1. Клонируйте проект
git clone <your-repo>
cd personal-assistant

# 2. Сделайте скрипты исполняемыми
chmod +x scripts/*.sh

# 3. Сгенерируйте секреты
make secrets

# 4. Запустите сервисы
make up

make up-dev  # Все сервисы + мониторинг

# Или все вместе:
make reset

# Посмотрите пароль в .env файле
cat .env | grep DB_PASSWORD

# Или используйте grep
grep 'DB_PASSWORD=' .env

# Запуск всех сервисов + мониторинг
make up-dev

# 1. Создать скрипты
mkdir -p scripts backups
# Скопировать скрипты выше

chmod +x scripts/*.sh

# 2. Добавить cron-backup в docker-compose.yml

# 3. Автоматизация
make cron-setup

# 4. Первый тест
make daily-backup
ls -lh backups/  # full-backup-YYYYMMDD.sql.gz

make up              # 🚀 Всё за 2 минуты
make status          # 📊 Статус + URL'ы
make validate        # 🔍 16+ таблиц ✓
make backup          # 💾 Ручной бэкап
make cron-setup      # ⏰ Автоматизация


"

3. Синхронизировать файл секрета

bash
echo "mypassword123" > docker-secrets/postgrespassword.txt
chmod 600 docker-secrets/postgrespassword.txt

